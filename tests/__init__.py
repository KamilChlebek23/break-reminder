"""Test suite for ``break_reminder``.

Mirrors the package layout: each ``tests/test_<module>.py`` file targets
exactly one module under ``break_reminder/``. Shared fixtures live in
``conftest.py``; helper stubs (``FakeVoice``, ``Clock``, …) are kept
file-local so per-suite drift stays contained.
"""
